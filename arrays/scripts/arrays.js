// Creating variable array.
var myArray = []; // Now is created free/empty array.
// Enteting elements into array
myArray[0] = 25; // We can add value on specific position but ordered by simply changing the array.In square brackets we are typing index which shows place where we want to make na entry to our array.
console.log(myArray);
myArray[1] = 50;
console.log(myArray);
myArray[2] = false;
console.log(myArray);
myArray[3] = "string or whatever";
console.log(myArray);
// Changing elements in array
myArray[2] = true; //That is one of the methods to entering or changing elements in arrays but are others.
//We can just write an array
var myArray2 = [2, 30, false, "one",'Two'];
// Or
var myArray3 = new Array(); // It creates empty array
// Or
var myArray4 = new Array(5); // It creates array with 5 empty slots only.
//---------------------------------------------------
// How to edit or take an actions to the array by using methodes
myArray2.length; // Checks the arrays length

myArray3.sort(); //Sorting elements in array

myArray4.reverse(); // Reverse the array.




