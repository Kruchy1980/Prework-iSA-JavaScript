// Firstly we need to declare variable
var myName = "Jakub";
// Now we can insert in document our variable by command
document.write(myName); // document is an object and "write" is a method.
// We can also enter numbers and string, math operators ana any math actions as well not only variables.